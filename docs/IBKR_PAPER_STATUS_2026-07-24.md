# IBKR Paper 状态

- Gateway 本地端口 `127.0.0.1:4002` 可达。
- 官方 TWS Python API 已从 D 盘安装源接入本地 Python 3.13 环境。
- API 只读开启，Paper 下单关闭。
- 账户与持仓读取会对账户号脱敏。
- 多个美股与 ETF 的历史日 K 曾成功取得；当前不含单一代码专用路径。
- 本轮大样本历史请求返回 IBKR 错误 162：同一交易用户存在来自其他 IP 地址的交易会话。

处理方式：

1. 退出其他电脑、手机或网页的同一 IBKR 交易会话。
2. 重新登录 Gateway Paper。
3. 在客户端“数据队列”重试失败任务。

该错误不影响已下载数据和离线扫描。备用公开日 K 只用于研究，不能代替 IBKR 实时行情或实盘成交数据。
